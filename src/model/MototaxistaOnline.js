class MototaxistaOnline {
    constructor(startSession,finishSession,clientesPorSession,startLocation,currentLocation,finishLocation) {
      this.startSession = startSession
      this.finishSession = finishSession
      this.clientesPorSession = clientesPorSession
      this.startLocation = startLocation
      this.currentLocation = currentLocation
      this.finishLocation = finishLocation
    }
  
  }

  export default (MototaxistaOnline) 